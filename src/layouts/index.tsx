import React from 'react';

import SurveyorchestratorLayouts from './surveyorchestrator';

const LayoutsRoot = (): React.JSX.Element => {
  return <SurveyorchestratorLayouts />;
};

export default LayoutsRoot;
